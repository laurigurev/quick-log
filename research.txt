research:
 - can you deal with compile time strings
 - can you collect compile time data separately
 - perfect forwarding???
 - function with compile time and runtime versions
 - __builtin_return_address AND _AddressOfReturnAddress
 - compile time tuple
 - tuple sizeof
 - memcpy tuple
 - serializer and deserializer
 - compile time string

qlog(int level, const char* str, ...);

processing:
 - static assert level
 - static assert string, needs template string class
 - static assert string format specifier count to variadic argument count
 - if first time, serialize level, string, id, args
 - else serialize id, args
